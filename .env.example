VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_API_BASE=http://localhost:8000

# Cloudflare Turnstile (Section 6.1) — leave unset for local dev; the
# TurnstileWidget component then auto-passes a dev placeholder token,
# matching the backend's CAPTCHA_PROVIDER=none bypass.
VITE_TURNSTILE_SITE_KEY=